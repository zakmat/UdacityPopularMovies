<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_margin="4dp"
              android:layout_marginBottom="12dp"
              android:background="@color/colorPrimaryLight"
              android:orientation="vertical">

    <ImageView
        android:id="@+id/trailer_icon"
        android:layout_width="@dimen/trailer_thumbnail_width"
        android:layout_height="@dimen/trailer_thumbnail_height"
        android:layout_gravity="center_horizontal"
        android:contentDescription="@string/placeholder_trailer_thumbnail"
        app:srcCompat="@mipmap/placeholder"
        tools:layout_height="@dimen/trailer_thumbnail_height"
        tools:layout_width="@dimen/trailer_thumbnail_width"
        />

    <TextView
        android:id="@+id/trailer_name"
        style="@style/Base.TextAppearance.AppCompat.Body1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:maxLines="3"
        android:minLines="3"
        android:paddingLeft="@dimen/trailers_caption_padding"
        android:paddingRight="@dimen/trailers_caption_padding"
        tools:text="@string/placeholder_trailer_name"/>
</LinearLayout>
